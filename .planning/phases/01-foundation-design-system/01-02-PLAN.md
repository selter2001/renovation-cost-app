---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/layout/Layout.tsx
  - src/components/layout/Navbar.tsx
  - src/components/theme-toggle.tsx
  - src/components/language-toggle.tsx
  - src/pages/HomePage.tsx
  - src/index.css
  - src/router/index.tsx
  - .github/workflows/deploy.yml
autonomous: false

must_haves:
  truths:
    - "Nawigacja zawiera logo RenoCost, przelacznik jezyka PL/EN, przelacznik motywu jasny/ciemny i placeholder na menu uzytkownika"
    - "Klikniecie przelacznika motywu zmienia wyglad strony miedzy jasnym a ciemnym natychmiast"
    - "Klikniecie przelacznika jezyka zmienia caly widoczny tekst miedzy PL i EN natychmiast"
    - "Interfejs jest responsywny -- na telefonie navbar uzywa hamburger menu z Sheet, na desktopie pokazuje elementy inline"
    - "Glassmorphism jest widoczny -- navbar ma rozmycie tla (backdrop-blur), karty maja polprzezroczyste tlo"
    - "Strona glowna wyswietla hero sekcje z tytulem, podtytulem i przyciskiem CTA -- wszystko przetlumaczone"
    - "GitHub Actions workflow deploy.yml istnieje i jest poprawnie skonfigurowany"
  artifacts:
    - path: "src/components/layout/Navbar.tsx"
      provides: "Responsywna nawigacja z logo, language toggle, theme toggle, user menu placeholder"
      min_lines: 50
    - path: "src/components/layout/Layout.tsx"
      provides: "Layout wrapper z Navbar + Outlet"
      min_lines: 10
    - path: "src/components/theme-toggle.tsx"
      provides: "Przycisk przelaczania motywu jasny/ciemny/system"
      exports: ["ModeToggle"]
    - path: "src/components/language-toggle.tsx"
      provides: "Przycisk przelaczania jezyka PL/EN"
      exports: ["LanguageToggle"]
    - path: "src/pages/HomePage.tsx"
      provides: "Hero sekcja z glassmorphism"
      contains: "useTranslation"
    - path: ".github/workflows/deploy.yml"
      provides: "CI/CD pipeline do GitHub Pages"
      contains: "deploy-pages"
  key_links:
    - from: "src/components/layout/Navbar.tsx"
      to: "src/components/theme-toggle.tsx"
      via: "import i renderowanie ModeToggle w navbar"
      pattern: "ModeToggle"
    - from: "src/components/layout/Navbar.tsx"
      to: "src/components/language-toggle.tsx"
      via: "import i renderowanie LanguageToggle w navbar"
      pattern: "LanguageToggle"
    - from: "src/components/theme-toggle.tsx"
      to: "src/components/theme-provider.tsx"
      via: "useTheme hook zmienia motyw"
      pattern: "useTheme"
    - from: "src/components/language-toggle.tsx"
      to: "i18next"
      via: "useTranslation hook + i18n.changeLanguage"
      pattern: "changeLanguage"
    - from: "src/components/layout/Layout.tsx"
      to: "src/components/layout/Navbar.tsx"
      via: "renderuje Navbar + Outlet"
      pattern: "Navbar"
    - from: "src/router/index.tsx"
      to: "src/components/layout/Layout.tsx"
      via: "Layout jako wrapper trasy"
      pattern: "Layout"
---

<objective>
Zbudowac widoczny interfejs uzytkownika: responsywna nawigacja z glassmorphism, przelaczniki motywu i jezyka, hero na stronie glownej, oraz pipeline deploy na GitHub Pages.

Purpose: Dostarczyc "gotowa powloke" aplikacji -- uzytkownik widzi profesjonalny, dzialajacy interfejs z nawigacja i przelacznikami.
Output: Kompletna nawigacja (desktop + mobile), strona glowna z hero, deploy workflow, aplikacja gotowa do uruchomienia Phase 2.
</objective>

<execution_context>
@/Users/wojciecholszak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wojciecholszak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Komponenty nawigacji, przelaczniki, layout i strona glowna</name>
  <files>
    src/components/layout/Navbar.tsx
    src/components/layout/Layout.tsx
    src/components/theme-toggle.tsx
    src/components/language-toggle.tsx
    src/pages/HomePage.tsx
    src/index.css
    src/router/index.tsx
  </files>
  <action>
    WAZNE: Przed rozpoczeciem tego taska przeczytaj SUMMARY z Plan 01 aby poznac dokladna strukture plikow, CSS variables i zainstalowane shadcn komponenty.

    1. Dodaj potrzebne shadcn komponenty:
       ```
       pnpm dlx shadcn@latest add dropdown-menu sheet
       ```
       (Button juz dodany w Plan 01)

    2. ModeToggle (src/components/theme-toggle.tsx):
       - Importuj useTheme z @/components/theme-provider
       - Uzyj DropdownMenu (shadcn) z trzema opcjami: Jasny, Ciemny, Systemowy
       - Ikona: Sun (light), Moon (dark) z lucide-react
       - Teksty opcji przez useTranslation: t("nav.theme.light"), t("nav.theme.dark"), t("nav.theme.system")
       - Eksportuj jako ModeToggle
       - Styl przycisku: variant="ghost" size="icon"

    3. LanguageToggle (src/components/language-toggle.tsx):
       - Wzorzec z 01-RESEARCH.md sekcja "Language Toggle komponent"
       - Przycisk wyswietla "EN" gdy aktywny PL, "PL" gdy aktywny EN (pokaz jaki jezyk ZMIENI SIE po kliknieciu)
       - Uzyj Button variant="ghost" size="sm"
       - Dodaj ikona Globe z lucide-react obok tekstu
       - Eksportuj jako LanguageToggle

    4. Navbar (src/components/layout/Navbar.tsx):
       - Wzorzec z 01-RESEARCH.md Pattern 6 (responsywna nawigacja)
       - GLASSMORPHISM na navbar: sticky top-0 z-50, backdrop-blur-lg, bg-background/80 (uzyj zmiennych shadcn), border-b border-border/40
       - Desktop (md:): flex items-center gap-4 z LanguageToggle, ModeToggle, placeholder div/span na UserMenu (tekst "Konto" / t("nav.userMenu") z ikona User z lucide)
       - Mobile (ponizej md:): LanguageToggle i ModeToggle widoczne, hamburger otwiera Sheet z nawigacja
       - Logo: tekst "RenoCost" z czcionka Montserrat font-bold text-xl, gradient lub kolor akcentu
       - Container: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
       - Wysokosc navbara: h-16

    5. Layout (src/components/layout/Layout.tsx):
       - Importuj Navbar
       - Renderuj: <div class="min-h-screen bg-background font-sans"> <Navbar /> <main class="container mx-auto px-4 py-8"> <Outlet /> </main> </div>
       - Outlet z react-router

    6. Zaktualizuj router (src/router/index.tsx):
       - Layout wrapper: element: <Layout /> (zamiast prostego div z Plan 01)
       - Importuj Layout z @/components/layout/Layout

    7. HomePage (src/pages/HomePage.tsx) -- kompletna hero sekcja:
       - useTranslation do wszystkich tekstow
       - Hero: wycentrowany blok z glassmorphism karta:
         - Tytul h1: t("home.title") -- duzy (text-4xl sm:text-5xl), Montserrat, font-bold
         - Podtytul p: t("home.subtitle") -- muted-foreground, text-lg
         - CTA Button: t("home.cta") -- duzy przycisk z kolorem akcentu, zaokraglone rogi (rounded-xl)
       - Glassmorphism card wokol hero: rounded-2xl border border-white/20 dark:border-white/10 bg-white/60 dark:bg-slate-900/60 backdrop-blur-md shadow-lg p-8 sm:p-12
       - Tlo strony: moze miec subtelny gradient (from-background to-muted) lub pattern

    8. Dopracuj src/index.css:
       - Upewnij sie ze font-family dla body to Inter (variable), dla h1-h6 to Montserrat (variable)
       - Dodaj klase .font-heading z font-family Montserrat
       - Upewnij sie ze glassmorphism wyglada dobrze w obu motywach
       - Dodaj plynne przejscie motywu: `* { transition: background-color 0.2s, border-color 0.2s, color 0.2s; }` (NIE na backdrop-blur -- to psuje performance)
       - Pamietaj o supports-[backdrop-filter] fallback
  </action>
  <verify>
    - `pnpm dev` -- nawigacja widoczna na gorze strony z logo, przelacznikami i placeholder konta
    - Klikniecie ModeToggle -> dropdown z 3 opcjami -> wybranie "Ciemny" -> strona ciemnieje natychmiast
    - Klikniecie LanguageToggle -> tekst na stronie zmienia sie PL<->EN natychmiast
    - Zmniejszenie okna przegladarki do 375px -> hamburger pojawia sie, Sheet otwiera sie po kliknieciu
    - Navbar ma widoczny efekt glassmorphism (rozmycie tla przy przewijaniu)
    - Hero karta ma widoczne glassmorphism (polprzezroczyste tlo, blur, cien)
    - `pnpm build` -- buduje bez bledow TypeScript
  </verify>
  <done>
    Nawigacja wyswietla logo RenoCost, dzialajacy przelacznik motywu (3 opcje), dzialajacy przelacznik jezyka PL/EN, placeholder konta. Responsywna: desktop inline, mobile hamburger+Sheet. Glassmorphism widoczny na navbarze i karcie hero. Strona glowna pokazuje przetlumaczony tytul, podtytul i CTA. Wszystko poprawne w obu motywach.
  </done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions deploy workflow</name>
  <files>
    .github/workflows/deploy.yml
  </files>
  <action>
    1. Stworz .github/workflows/deploy.yml:
       - Uzyj dokladnego wzorca z 01-RESEARCH.md Pattern 7
       - Trigger: push to main + workflow_dispatch
       - Permissions: contents read, pages write, id-token write
       - Concurrency: group "pages", cancel-in-progress true
       - Steps:
         a. actions/checkout@v5
         b. actions/setup-node@v6 z cache: "pnpm" i node-version: "lts/*"
         c. WAZNE -- pnpm setup: dodaj krok `uses: pnpm/action-setup@v4` PRZED setup-node (potrzebny do cache)
         d. `pnpm install --frozen-lockfile`
         e. `pnpm run build`
         f. actions/configure-pages@v5
         g. actions/upload-pages-artifact@v4 z path: "./dist"
         h. actions/deploy-pages@v4 z id: deployment
       - Environment: github-pages z url: ${{ steps.deployment.outputs.page_url }}

    2. Upewnij sie ze pnpm-lock.yaml bedzie commitowany (sprawdz .gitignore -- NIE MOZE zawierac pnpm-lock.yaml)

    UWAGA: Ten workflow ZADZIALA dopiero po wlaczeniu GitHub Pages w ustawieniach repozytorium (Settings > Pages > Source: GitHub Actions). To jest krok ktory uzytkownik musi wykonac recznie -- NIE dodawaj tego jako checkpoint, zanotuj w SUMMARY.
  </action>
  <verify>
    - Plik .github/workflows/deploy.yml istnieje i ma poprawna skladnie YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"`
    - Workflow uzywa pnpm (nie npm)
    - Workflow buduje z `pnpm run build` i uploaduje `./dist`
    - `pnpm build` nadal dziala po wszelkich zmianach
  </verify>
  <done>
    GitHub Actions workflow deploy.yml istnieje, jest poprawnie skonfigurowany z pnpm, buduje projekt i deployuje do GitHub Pages. SUMMARY dokumentuje ze uzytkownik musi wlaczyc Pages w ustawieniach repo.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Kompletny interfejs Phase 1: responsywna nawigacja z glassmorphism (logo, przelacznik jezyka PL/EN, przelacznik motywu jasny/ciemny/system, placeholder konta), strona glowna z hero sekcja, oraz GitHub Actions deploy workflow.
  </what-built>
  <how-to-verify>
    1. Uruchom `pnpm dev` i otworz http://localhost:5173 w przegladarce
    2. Sprawdz nawigacje:
       - Logo "RenoCost" widoczne po lewej
       - Przelacznik jezyka -- kliknij, sprawdz czy CALY tekst zmienia sie PL<->EN
       - Przelacznik motywu -- kliknij, wybierz Ciemny, sprawdz czy strona ciemnieje bez migniecia
       - Odswiez strone -- motyw i jezyk powinny sie zachowac
    3. Sprawdz responsywnosc:
       - Zmniejsz okno do 375px szerokosci (lub DevTools device mode iPhone)
       - Hamburger menu powinno sie pojawic, Sheet otwiera sie po kliknieciu
    4. Sprawdz estetyka:
       - Glassmorphism na navbarze (rozmycie tla widoczne gdy przewijasz)
       - Hero karta z polprzezroczystym tlem
       - Fonty Inter (body) i Montserrat (naglowki)
       - Paleta granat/grafit z akcentem
    5. Sprawdz tryb ciemny:
       - Przelacz na ciemny -- czy wyglada profesjonalnie?
       - Odswiez (F5) -- czy NIE ma bialego migniecia?
  </how-to-verify>
  <resume-signal>Wpisz "approved" jesli wszystko wyglada dobrze, lub opisz problemy do poprawy</resume-signal>
</task>

</tasks>

<verification>
1. Nawigacja: logo + language toggle + theme toggle + user placeholder -- wszystko widoczne i dzialajace
2. Responsywnosc: desktop inline, mobile hamburger + Sheet
3. Glassmorphism: navbar i hero card maja backdrop-blur, polprzezroczyste tla, cienie
4. Dark mode: dziala, persystuje, zero FOUC
5. i18n: PL/EN przelaczanie natychmiastowe, persystencja po refresh
6. Typografia: Inter (body), Montserrat (headings)
7. Paleta: granat/grafit z akcentem blekit/szmaragd
8. `pnpm build` -- zero bledow
9. deploy.yml -- poprawna skladnia, pnpm, dist upload
</verification>

<success_criteria>
- Uzytkownik widzi profesjonalna nawigacje z dzialajacymi przelacznikami jezyka i motywu
- Interfejs wyglada dobrze na telefonie (375px) i desktopie (1440px)
- Glassmorphism widoczny na navbarze i hero card
- Dark mode dziala bez FOUC, persystuje
- i18n PL/EN dziala, persystuje
- GitHub Actions workflow gotowy do deploy (po wlaczeniu Pages w ustawieniach repo)
- `pnpm build` buduje bez bledow
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
